package lixir.vminus.network;

import lixir.vminus.VMinus;
import net.minecraft.util.Identifier;

public class PacketIdentifiers {
    public static final Identifier CAPE_MENU_PACKET_IDENTIFIER = new Identifier(VMinus.MOD_ID, "cape");
    public static final Identifier DEFAULT_MENU_PACKET_IDENTIFIER = new Identifier(VMinus.MOD_ID, "default");
}
